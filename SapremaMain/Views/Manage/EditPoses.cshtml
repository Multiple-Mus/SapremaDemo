@model EditPosesViewModel

<h1 class="page-header">EditPoses</h1>
    <div>
        <h1 class="page-header">Poses</h1>
    </div>
    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
        <div class="panel-group">
            <div class="panel panel-default">
                @*@foreach (var poseTheme in Model.PoseThemes)
                {
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <a data-toggle="collapse" href="#@poseTheme.PoseTheme">@poseTheme.PoseTheme</a>
                        </h4>
                    </div>
                    <div id="@poseTheme.PoseTheme" class="panel-collapse collapse">
                        
                        <ul class="list-group">

                            @foreach (var themeData in poseTheme.PoseList)
                            {
                                <li id="drag1" class="list-group-item">
                                    @themeData.PoseName
                                    <label class="switch">
                                        <input type="checkbox" checked/>
                                        <div class="slider round"></div>
                                    </label>
                                </li>
                            }
                        </ul>
                    </div>
                }*@
            </div>
        </div>
    </div>


@*@{int i = 1; }
    @foreach (var v in Model.PoseThemes)
    {
        {i++;}
        <div>@i</div>

    }*@
